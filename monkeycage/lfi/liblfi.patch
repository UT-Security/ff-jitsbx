diff --git a/liblfi/host/linux/random.c b/liblfi/host/linux/random.c
index fc3ca368c7..086ee5eaca 100644
--- a/liblfi/host/linux/random.c
+++ b/liblfi/host/linux/random.c
@@ -1,9 +1,19 @@
 #include <errno.h>
-#include <sys/random.h>
+#include <unistd.h>
 
 #include "host/error.h"
 #include "host.h"
 
+#define SYS_getrandom 318
+
+/* Flags for use with getrandom.  */
+#define GRND_NONBLOCK 0x01
+#define GRND_RANDOM 0x02
+
+static ssize_t getrandom(void* buf, size_t buflen, unsigned int flags) {
+    return syscall(SYS_getrandom, buf, buflen, flags);
+}
+
 static unsigned int
 linuxflags(unsigned int flags)
 {
diff --git a/liblfi/types.h b/liblfi/types.h
index b5a51e65c7..fa1e0d7b78 100644
--- a/liblfi/types.h
+++ b/liblfi/types.h
@@ -2,7 +2,7 @@
 
 #include <stdint.h>
 
-#define EXPORT __attribute__((visibility("default")))
+#define EXPORT
 
 typedef uint64_t  u64;
 typedef uint32_t  u32;
diff --git a/lfi/liblfi/pal/platform.c b/lfi/liblfi/pal/platform.c
index c0ab8cf56d..5520320af5 100644
--- a/lfi/liblfi/pal/platform.c
+++ b/lfi/liblfi/pal/platform.c
@@ -28,13 +28,13 @@ lfi_new_plat(struct LFIPlatOptions opts)
         goto err1;

     struct BoxMap* bm = boxmap_new((struct BoxMapOptions) {
-        .minalign = gb(512),
-        .maxalign = gb(512),
+        .minalign = gb(256),
+        .maxalign = gb(256),
         .guardsize = gb(0),
     });
     if (!bm)
         goto err2;
-    if (!boxmap_reserve(bm, gb(1024)))
+    if (!boxmap_reserve(bm, gb(512)))
         goto err3;
